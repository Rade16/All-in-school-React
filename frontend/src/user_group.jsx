import React from 'react';
import ReactDOM from 'react-dom/client';
import User_group from "./full_pages/user_group";

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <User_group />
  </React.StrictMode>,
);