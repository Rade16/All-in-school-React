import React from 'react';
import ReactDOM from 'react-dom/client';
import TestsPage from "./full_pages/tests";

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TestsPage />
  </React.StrictMode>,
);