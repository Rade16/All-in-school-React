import React from 'react';
import ReactDOM from 'react-dom/client';
import Settings_page from "./full_pages/settings";

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Settings_page />
  </React.StrictMode>,
);