import React from 'react';
import ReactDOM from 'react-dom/client';
import User_courses from "./full_pages/user_courses";

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <User_courses />
  </React.StrictMode>,
);