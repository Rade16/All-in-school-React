import React from 'react';
import ReactDOM from 'react-dom/client';
import User_profile from "./full_pages/user_profile";

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <User_profile />
  </React.StrictMode>,
);
