import React from 'react';
import ReactDOM from 'react-dom/client';
import TestsResultsPage from "./full_pages/testsResults";

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TestsResultsPage />
  </React.StrictMode>,
);