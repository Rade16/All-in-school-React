import React from 'react';
import ReactDOM from 'react-dom/client';
import With_auth_catalog from "./full_pages/with_auth_catalog";

ReactDOM.createRoot(document.getElementById('auth')).render(
  <React.StrictMode>
    <With_auth_catalog />
  </React.StrictMode>,
);
