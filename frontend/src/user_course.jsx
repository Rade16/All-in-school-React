import React from 'react';
import ReactDOM from 'react-dom/client';
import WithAuthCourse from "./full_pages/with_auth_course";

ReactDOM.createRoot(document.getElementById('auth')).render(
  <React.StrictMode>
    <WithAuthCourse />
  </React.StrictMode>,
);
