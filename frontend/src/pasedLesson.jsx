import React from 'react';
import ReactDOM from 'react-dom/client';
import PasedLesson from "./full_pages/pasedLesson";

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PasedLesson />
  </React.StrictMode>,
);