import React from 'react';
import ReactDOM from 'react-dom/client';
import Lesson from "./full_pages/lesson";

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Lesson />
  </React.StrictMode>,
);