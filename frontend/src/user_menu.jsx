import React from 'react';
import ReactDOM from 'react-dom/client';
import With_auth_menu from "./full_pages/with_auth_menu";

ReactDOM.createRoot(document.getElementById('auth')).render(
  <React.StrictMode>
    <With_auth_menu />
  </React.StrictMode>,
);
